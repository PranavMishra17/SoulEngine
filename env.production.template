# SoulEngine - Production Environment
# Set these as environment variables in your deployment platform (Render, etc.)

# Server Configuration
NODE_ENV=production
PORT=3000
LOG_LEVEL=info

# ============================================
# REQUIRED: Supabase Configuration
# ============================================
SUPABASE_URL=your-supabase-url
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# ============================================
# REQUIRED: Encryption Key
# Must be at least 32 characters - used to encrypt API keys stored in database
# Generate a secure random key for production!
# ============================================
ENCRYPTION_KEY=your-production-encryption-key-32chars

# Session Configuration
SESSION_TIMEOUT_MS=1800000

# State History
STATE_HISTORY_ENABLED=true
STATE_HISTORY_MAX_VERSIONS=10

# Limits
MAX_NPCS_PER_PROJECT=10
MAX_CATEGORIES=20
MAX_DEPTH_TIERS=5
MAX_CONCURRENT_SESSIONS=100
MAX_STM_MEMORIES=20
MAX_LTM_MEMORIES=50

# Security
MAX_INPUT_LENGTH=500
RATE_LIMIT_PER_MINUTE=10

# ============================================
# OPTIONAL: Default Provider API Keys
# NOT REQUIRED - This is a BYOK (Bring Your Own Key) system
# Users provide their own API keys via project settings
# Only set these if you want server-wide defaults
# ============================================

# LLM Provider (gemini, openai, anthropic, grok)
DEFAULT_LLM_PROVIDER=gemini

# LLM API Keys (NOT REQUIRED)
# GEMINI_API_KEY=
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GROK_API_KEY=

# Voice API Keys (NOT REQUIRED)
# DEEPGRAM_API_KEY=
# CARTESIA_API_KEY=
# ELEVENLABS_API_KEY=
